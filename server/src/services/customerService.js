import { getCollection, COLLECTIONS } from '../config/database.js';

export const customerService = {
  async getAllCustomers() {
    // Architecture ready for Firestore query on customers collection
    return [];
  },
};

export default customerService;
