import { getCollection, COLLECTIONS } from '../config/database.js';

export const purchaseService = {
  async getAllPurchases() {
    // Architecture ready for Firestore query on purchases collection
    return [];
  },
};

export default purchaseService;
