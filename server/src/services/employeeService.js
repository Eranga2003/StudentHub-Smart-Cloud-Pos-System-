import { getCollection, COLLECTIONS } from '../config/database.js';

export const employeeService = {
  async getAllEmployees() {
    // Architecture ready for Firestore query on employees collection
    return [];
  },
};

export default employeeService;
