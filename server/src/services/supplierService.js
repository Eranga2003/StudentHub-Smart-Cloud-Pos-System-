import { getCollection, COLLECTIONS } from '../config/database.js';

export const supplierService = {
  async getAllSuppliers() {
    // Architecture ready for Firestore query on suppliers collection
    return [];
  },
};

export default supplierService;
