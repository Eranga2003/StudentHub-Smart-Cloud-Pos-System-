import { getCollection, COLLECTIONS } from '../config/database.js';

export const expenseService = {
  async getAllExpenses() {
    // Architecture ready for Firestore query on expenses collection
    return [];
  },
};

export default expenseService;
