import { getCollection, COLLECTIONS } from '../config/database.js';

export const salesService = {
  async getAllSales() {
    // Architecture ready for Firestore query on sales collection
    return [];
  },
};

export default salesService;
